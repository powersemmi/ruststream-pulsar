[package]
name = "ruststream-pulsar"
description = "Apache Pulsar broker implementation for the RustStream messaging framework."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
readme = "../../README.md"
keywords = ["pulsar", "messaging", "streaming", "broker", "ruststream"]
categories = ["asynchronous", "network-programming"]

[lints]
workspace = true

[features]
default = []
# The in-process test broker (TestableBroker + TestApp support). The conformance harness is a
# broker-author tool and stays in this repo's dev-dependencies, not in the user-facing feature.
testing = ["ruststream/testing"]

[dependencies]
ruststream = { workspace = true, default-features = false }
pulsar.workspace = true
regex.workspace = true
bytes.workspace = true
futures.workspace = true
thiserror.workspace = true
tokio.workspace = true
tracing.workspace = true

[dev-dependencies]
# `cbor` is a second codec for the tests alone: a publish that names a per-message setting has to
# leave in the format its mount site named, and one codec cannot tell that apart from the default.
ruststream = { workspace = true, features = [
    "cbor",
    "conformance",
    "testing",
    "macros",
    "json",
] }
# Not a bare "1": `serde_derive` below this cannot parse a `#[derive(Serialize)]` type that also
# carries `#[outgoing(headers = Meta)]`, whose value is a path rather than a literal, so the
# doctests fail to expand. The floor the minimal-versions job verifies.
serde = { version = "1.0.103", features = ["derive"] }
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "sync", "time"] }
